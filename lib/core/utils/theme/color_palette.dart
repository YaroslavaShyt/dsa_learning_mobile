import 'package:flutter/material.dart';

class ColorPalette {
  // PURPLE
  static const rebeccaPurple = Color(0xff5C3E84);

  // YELLOW
  static const maize = Color(0xfffff27d);

  // PINK
  static const crayola = Color(0xfff35c6e);

  // GREEN
  static const mintGreen = Color(0xffCCFBFE);

  // WHITE
  static final white = Color(0xffffffff).withValues(alpha: 0.83);

  // BLACK
  static final shadow = Color(0x00000000).withValues(alpha: 0.25);
}
